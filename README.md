# NodeJS 2025 Q4 CRUD-API

### Before start to work check need to install all dependencies from `package.json` file

```sh
npm install
```

To start work in development mode:

```sh
npm run start:dev
```

To start work in production mode:

```sh
npm run start:prod
```

To run linter:

```sh
npm run lint
```